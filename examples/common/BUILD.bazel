load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "common",
    srcs = glob(["**/*.cpp"]),
    hdrs = glob(["**/*.h"]),
    cxxopts = ["-std=c++20"],
    defines = [
        "ENTRY_CONFIG_USE_WAYLAND=1",
    ],
    strip_include_prefix = ".",
    visibility = ["//:__subpackages__"],
    deps = [
        "//:bgfx",
        "//3rdparty/dear-imgui",
        "//3rdparty/iconfontheaders",
        "//3rdparty/meshoptimizer",
        "//3rdparty/sdf",
        "//3rdparty/stb",
        "@bimg",
        "@bx",
    ],
)
