load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "texturev",
    srcs = glob([
        "*.cpp",
        "*.h",
    ]),
    copts = ["-march=native"],
    cxxopts = ["-std=c++20"],
    deps = ["//examples/common"],
)
