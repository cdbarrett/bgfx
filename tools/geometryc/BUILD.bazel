load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "geometryc",
    srcs = glob(["*.cpp"]),
    copts = ["-march=native"],
    cxxopts = ["-std=c++20"],
    visibility = ["//visibility:public"],
    deps = [
        "//:bgfx",
        "//3rdparty/cgltf",
        "//3rdparty/meshoptimizer",
        "@bx",
    ],
)
